== README

* 简单购物项目,支持登录注册（邮箱验证），后台管理，添加购物车，创建订单

* 注册页面填写实际邮箱验证，也可以手动激活

```Ruby

rails c 
user = User.last
user.is_active = true
user.save

```

* 后台管理，需要手动设置管理员

```Ruby

rails c 
user = User.last
user.is_admin = true
user.save

```
* 目前为止还没更多的介绍